interface:
  display_name: "Architecture Change Planning"
  short_description: "Submit reviewable architecture plans via MCP"
  default_prompt: "Use $architecture-change-plan to turn this request into an evidence-backed proposal and submit it to the viewer through MCP."
