interface:
  display_name: "Architecture Change Planning"
  short_description: "Submit reviewable architecture plans via MCP"
  default_prompt: "Use $architecture-change-plan to turn this request or confirmed concept discussion into a basis-backed target proposal and submit it to the viewer through MCP without claiming the target is already implemented."
