interface:
  display_name: "Project Architecture Discovery"
  short_description: "Submit evidence-backed architecture snapshots via MCP"
  default_prompt: "Use $architecture-discovery to inspect this repository and submit an evidence-backed architecture snapshot to the viewer through MCP."
