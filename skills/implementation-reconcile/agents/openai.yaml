interface:
  display_name: "Implementation Reconciliation"
  short_description: "Submit implementation evidence and drift via MCP"
  default_prompt: "Use $implementation-reconcile to compare the code with the approved architecture and submit the observed result through MCP."
