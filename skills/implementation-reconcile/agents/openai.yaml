interface:
  display_name: "Implementation Reconciliation"
  short_description: "Compare implemented code with an approved architecture"
  default_prompt: "Use $implementation-reconcile to compare the implemented code with the approved architecture and report any drift."
