# Keep real values in your shell, deployment secret store, or an ignored local .env file.
# This application reads process environment variables; it does not expose them to the browser.

# Required only to generate AI proposals.
DEEPSEEK_API_KEY=

# Optional provider settings.
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash

# Optional local runtime settings.
PORT=8800
# VIEWER_PROJECT_DIR=projects/demo
